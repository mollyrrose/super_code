"""MCP servers spawned by Claude Code for the super_code AI video pipeline.

- comfyui_mcp: local ComfyUI driver (Tier C: RTX 4070 8 GB, LTX-Video 0.9.8 fp8).
- sora_mcp:    cloud OpenAI Sora driver.

Register each with `claude mcp add ...` — see each module's docstring.
"""
